<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Llantas</title>
    <link rel="stylesheet" href="https://bootswatch.com/5/simplex/bootstrap.min.css">
    <link rel="icon" type="image/favicon" href="/favicon.jpg"/>
</head>
<body>
  <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="collapse navbar-collapse" id="navbarColor01">
      <a class="navbar-brand" href="http://localhost:3000">Llantas de skate</a>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="http://localhost:3000/Ecategoria"><img src="images/flecha.png" alt="Flecha" width="30" height="20"></a>
        </li>
      </ul>
    </div>
    </div>
  </nav>
    <table class="table table">
        <thead>
          <tr class="table-primary">
            <td>NO.</td>
            <td>Nombre</td>
            <td>Precio</td>
            <td>Descripcion</td>
            <td>Categoria</td>
          </tr>
        </thead>
        <tbody>
        <% if (data) { %>
          <% for(var i = 0; i < data.length; i++) { %>
            <tr>
              <td><%= (i + 1) %></td>
              <td><%= data[i].nombre %></td>
              <td><%= data[i].precio %></td>
              <td><%= data[i].descripcion %></td>
              <td><%= data[i].categoria %></td>
            </tr>
          <% } %>
        <% } %>
        </tbody>
      </table>
  <style>
    table, tr, td {
    border: 1px solid;
    }
      </style>

<%- include('footer.ejs') %>